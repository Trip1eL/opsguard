# OpsGuard：面向安全告警分析的多智能体调查与处置 Agent 原型

GitHub：https://github.com/Trip1eL/opsguard

## 项目简介

个人安全 Agent 原型，面向主机、Web、进程和网络等多源安全告警，完成行为关联、
MITRE ATT&CK 证据映射、Sigma 候选规则生成、Docker 容器隔离验证，并实现风险分级、
人工审批、灰度回滚和反馈优化的可重复 Demo 流程。

## 技术栈

Python、FastAPI、LangGraph、Elasticsearch/OpenSearch、Neo4j、Docker、Sigma、
MITRE ATT&CK、Pydantic、LangSmith

## 主要技术实现亮点

- **多智能体编排：** 设计 Orchestrator、专项 Agent 和受控工具协作架构，使用 LangGraph 节点图与显式状态机串联调查、行为关联、威胁映射、规则生成和验证流程；通过工具白名单、结构化输入输出、审批门禁和审计记录约束 Agent 行为，LLM 仅负责理解与规划。
- **事件模型与行为图谱：** 构建统一安全事件模型，关联主机、用户、进程、文件、IP 和域名等实体；实现 OpenSearch Repository 的索引映射、bulk 写入和结构化检索，并使用 Neo4j Driver Adapter 持久化行为图、进程父子链和攻击路径，保留 JSONL/内存 fallback。
- **项目内 ATT&CK 映射：** 维护项目所需的版本化 ATT&CK 技术子集，为 Technique 绑定关联事件、映射依据和置信度，结合行为图谱识别攻击阶段和检测覆盖缺口，使研判结论能够回指原始事件。
- **规则生成与容器沙箱验证：** 根据行为链生成版本化 Sigma 候选规则，在受限语法校验后通过 Docker Worker 执行无网络、只读文件系统、丢弃 capabilities、资源和超时受限的离线回放；基于标注数据计算 Precision、Recall、F1 和误报率，并记录失败样本。
- **策略生命周期方案：** 设计 `DRAFT -> VALIDATED -> CANARY -> ACTIVE -> ROLLED_BACK` 状态流转，支持按调查作用域提交灰度指标，并依据评估样本数、误报率、错误率和 P95 延迟设置暂停与自动回滚条件。
- **受控响应与数据飞轮：** 为主机隔离、账号禁用和域名阻断提供仅修改进程内状态的模拟适配器，高风险操作必须经过带作用域校验的人工审批；结合人工标注、规则评测和历史调查结果生成候选优化规则，重新验证后才可进入下一轮灰度。
