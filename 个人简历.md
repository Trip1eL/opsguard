# OpsGuard：面向生产系统的多智能体异常行为调查与自动化处置平台

GitHub：https://github.com/Trip1eL/opsguard

## 项目简介

面向企业生产系统异常行为分析与安全运营的 Multi-Agent 平台，接入主机、Web、进程和网络等多源事件，自动完成行为关联、攻击链还原、MITRE ATT&CK 映射、检测规则生成、沙箱验证及分级响应，形成从告警调查到策略迭代的可审计闭环。

## 技术栈

Python、FastAPI、LangGraph、Pydantic、Elasticsearch/OpenSearch、Neo4j、PostgreSQL、Redis、Docker、Sigma、MITRE ATT&CK、React + TypeScript

## 主要技术实现亮点

- 设计 `Orchestrator + Specialist Agents + Controlled Tools` 架构，通过 LangGraph 和显式状态机编排调查、行为关联、威胁映射、规则生成、验证和响应 Agent；使用工具白名单、结构化输入输出、审批门禁和审计轨迹约束 Agent 行为。
- 构建统一安全事件模型，关联主机、用户、进程、文件、IP 和域名等实体；使用 Elasticsearch/OpenSearch 完成日志检索与聚合，使用 Neo4j 还原跨数据源行为链、进程父子链和攻击路径，并输出可追溯证据。
- 实现基于证据的 MITRE ATT&CK 映射，为每项 Technique 记录关联事件、映射依据和置信度；结合历史案例与行为图谱识别攻击阶段和检测覆盖缺口。
- 根据行为链自动生成版本化 Sigma 规则，并在隔离 Docker 沙箱内完成语法检查、资源限制和离线回放；基于标注数据计算 Precision、Recall、F1 和误报率，将失败样本反馈至规则优化流程。
- 实现 `DRAFT -> VALIDATED -> CANARY -> ACTIVE -> ROLLED_BACK` 策略生命周期，支持按主机、业务范围和数据比例灰度发布；持续监控命中量、有效告警率和查询延迟，在误报或性能异常时自动暂停并回滚。
- 设计分级响应与 Human-in-the-loop 机制，高风险的主机隔离、账号禁用和域名阻断仅通过模拟适配器执行并要求人工审批；结合人工标注、规则指标和历史调查结果构建数据飞轮，自动生成候选优化规则，经验证和审批后进入下一轮灰度。

